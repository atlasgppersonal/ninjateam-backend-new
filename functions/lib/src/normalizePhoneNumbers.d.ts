/**
 * Normalizes a phone number to a 10-digit string (e.g., "1234567890").
 * Returns null if the number cannot be normalized.
 */
export declare function normalizePhoneNumber(phoneNumber: string | null | undefined): string | null;
