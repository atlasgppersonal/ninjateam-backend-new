module.exports = {
  root: true,
  env: {
    es6: true,
    node: true,
  },
  extends: [
  ],
  ignorePatterns: [
    "/lib/**/*", // Ignore built files.
    "/generated/**/*", // Ignore generated files.
  ],
  rules: {
  },
};
